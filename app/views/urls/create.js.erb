var url = '<%= "#{request.base_url}/" %>';
url += $('#custom-link').length ? '<%= "#{@url.custom}"%>' : '<%= "#{@url.short}"%>';
$('#link-input').val(url);
$('#submit-url').hide();
$('#copy-url').show();
$('.errors-container').hide();
